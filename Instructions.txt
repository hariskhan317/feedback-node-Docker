1st Step
Build image bu using following command
docker build -t feedbackimage .


###################################################
2nd Step
Build Container bu using following command
docker run -p 3000:80 --name feedback-app -d --rm -v feedbackvolume:/app/feedback feedbackimage:latest
